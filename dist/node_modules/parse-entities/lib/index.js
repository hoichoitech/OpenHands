import { decodeNamedCharacterReference as e } from "../../decode-named-character-reference/index.dom.js";
import { characterEntitiesLegacy as t } from "../../character-entities-legacy/index.js";
import { characterReferenceInvalid as n } from "../../character-reference-invalid/index.js";
import { isDecimal as r } from "../../is-decimal/index.js";
import { isHexadecimal as i } from "../../is-hexadecimal/index.js";
import { isAlphanumerical as a } from "../../is-alphanumerical/index.js";
//#region node_modules/parse-entities/lib/index.js
var o = [
	"",
	"Named character references must be terminated by a semicolon",
	"Numeric character references must be terminated by a semicolon",
	"Named character references cannot be empty",
	"Numeric character references cannot be empty",
	"Named character references must be known",
	"Numeric character references cannot be disallowed",
	"Numeric character references cannot be outside the permissible Unicode range"
];
function s(s, u) {
	let d = u || {}, f = typeof d.additional == "string" ? d.additional.charCodeAt(0) : d.additional, p = [], m = 0, h = -1, g = "", _, v;
	d.position && ("start" in d.position || "indent" in d.position ? (v = d.position.indent, _ = d.position.start) : _ = d.position);
	let y = (_ ? _.line : 0) || 1, b = (_ ? _.column : 0) || 1, x = C(), S;
	for (m--; ++m <= s.length;) if (S === 10 && (b = (v ? v[h] : 0) || 1), S = s.charCodeAt(m), S === 38) {
		let o = s.charCodeAt(m + 1);
		if (o === 9 || o === 10 || o === 12 || o === 32 || o === 38 || o === 60 || Number.isNaN(o) || f && o === f) {
			g += String.fromCharCode(S), b++;
			continue;
		}
		let u = m + 1, h = u, _ = u, v;
		if (o === 35) {
			_ = ++h;
			let e = s.charCodeAt(_);
			e === 88 || e === 120 ? (v = "hexadecimal", _ = ++h) : v = "decimal";
		} else v = "named";
		let y = "", E = "", D = "", O = v === "named" ? a : v === "decimal" ? r : i;
		for (_--; ++_ <= s.length;) {
			let n = s.charCodeAt(_);
			if (!O(n)) break;
			D += String.fromCharCode(n), v === "named" && t.includes(D) && (y = D, E = e(D));
		}
		let k = s.charCodeAt(_) === 59;
		if (k) {
			_++;
			let t = v === "named" ? e(D) : !1;
			t && (y = D, E = t);
		}
		let A = 1 + _ - u, j = "";
		if (!(!k && d.nonTerminated === !1)) if (!D) v !== "named" && w(4, A);
		else if (v === "named") {
			if (k && !E) w(5, 1);
			else if (y !== D && (_ = h + y.length, A = 1 + _ - h, k = !1), !k) {
				let e = y ? 1 : 3;
				if (d.attribute) {
					let t = s.charCodeAt(_);
					t === 61 ? (w(e, A), E = "") : a(t) ? E = "" : w(e, A);
				} else w(e, A);
			}
			j = E;
		} else {
			k || w(2, A);
			let e = Number.parseInt(D, v === "hexadecimal" ? 16 : 10);
			if (c(e)) w(7, A), j = "�";
			else if (e in n) w(6, A), j = n[e];
			else {
				let t = "";
				l(e) && w(6, A), e > 65535 && (e -= 65536, t += String.fromCharCode(e >>> 10 | 55296), e = 56320 | e & 1023), j = t + String.fromCharCode(e);
			}
		}
		if (j) {
			T(), x = C(), m = _ - 1, b += _ - u + 1, p.push(j);
			let e = C();
			e.offset++, d.reference && d.reference.call(d.referenceContext || void 0, j, {
				start: x,
				end: e
			}, s.slice(u - 1, _)), x = e;
		} else D = s.slice(u - 1, _), g += D, b += D.length, m = _ - 1;
	} else S === 10 && (y++, h++, b = 0), Number.isNaN(S) ? T() : (g += String.fromCharCode(S), b++);
	return p.join("");
	function C() {
		return {
			line: y,
			column: b,
			offset: m + ((_ ? _.offset : 0) || 0)
		};
	}
	function w(e, t) {
		let n;
		d.warning && (n = C(), n.column += t, n.offset += t, d.warning.call(d.warningContext || void 0, o[e], n, e));
	}
	function T() {
		g &&= (p.push(g), d.text && d.text.call(d.textContext || void 0, g, {
			start: x,
			end: C()
		}), "");
	}
}
function c(e) {
	return e >= 55296 && e <= 57343 || e > 1114111;
}
function l(e) {
	return e >= 1 && e <= 8 || e === 11 || e >= 13 && e <= 31 || e >= 127 && e <= 159 || e >= 64976 && e <= 65007 || (e & 65535) == 65535 || (e & 65535) == 65534;
}
//#endregion
export { s as parseEntities };

//# sourceMappingURL=index.js.map