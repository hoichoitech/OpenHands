import { HttpClient as e } from "./http-client.js";
//#region node_modules/@openhands/typescript-client/dist/client/server-client.js
var t = class {
	constructor(t) {
		this.host = t.host.replace(/\/$/, ""), this.apiKey = t.apiKey, this.client = new e({
			baseUrl: this.host,
			apiKey: this.apiKey,
			timeout: t.timeout || 6e4
		});
	}
	async getRoot() {
		return (await this.client.get("/")).data;
	}
	async getAlive() {
		return (await this.client.get("/alive")).data;
	}
	async getHealth() {
		return (await this.client.get("/health")).data;
	}
	async getReady() {
		return (await this.client.get("/ready", { acceptableStatusCodes: new Set([200, 503]) })).data;
	}
	async getServerInfo() {
		return (await this.client.get("/server_info")).data;
	}
	close() {
		this.client.close();
	}
};
//#endregion
export { t as ServerClient };

//# sourceMappingURL=server-client.js.map