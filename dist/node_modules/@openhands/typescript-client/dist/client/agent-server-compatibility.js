//#region node_modules/@openhands/typescript-client/dist/client/agent-server-compatibility.js
var e = "AGENT_SERVER_VERSION_TOO_OLD", t = {
	workspaces: {
		feature: "workspaces",
		displayName: "Workspaces",
		minVersion: "1.23.0"
	},
	hooks: {
		feature: "hooks",
		displayName: "Hooks API",
		minVersion: "1.23.0"
	},
	mcpTest: {
		feature: "mcp-test",
		displayName: "MCP test API",
		minVersion: "1.23.0"
	},
	mcpOAuth: {
		feature: "mcp-oauth",
		displayName: "MCP OAuth API",
		minVersion: "1.31.0"
	}
}, n = class t extends Error {
	constructor({ requirement: n, actualVersion: r }) {
		super(`${n.displayName} requires agent-server ${n.minVersion} or newer; this backend is running ${r}. Please upgrade your agent-server backend.`), this.code = e, this.name = "AgentServerVersionError", this.feature = n.feature, this.displayName = n.displayName, this.requiredVersion = n.minVersion, this.actualVersion = r, Object.setPrototypeOf(this, t.prototype);
	}
}, r = /* @__PURE__ */ new WeakMap();
function i(e) {
	return e instanceof n || typeof e == "object" && !!e && "code" in e && e.code === "AGENT_SERVER_VERSION_TOO_OLD";
}
async function a(e) {
	let t = r.get(e);
	if (t) return t;
	let n = e.get("/server_info").then((e) => e.data);
	return r.set(e, n), n;
}
async function o(e, t) {
	let r = await a(e), i = typeof r.version == "string" && r.version.trim() ? r.version.trim() : "unknown", o = s(i, t.minVersion);
	if (o === null || o < 0) throw new n({
		requirement: t,
		actualVersion: i
	});
	return r;
}
function s(e, t) {
	let n = c(e), r = c(t);
	if (!n || !r) return null;
	for (let e of [
		"major",
		"minor",
		"patch"
	]) {
		if (n[e] > r[e]) return 1;
		if (n[e] < r[e]) return -1;
	}
	return n.prerelease && !r.prerelease ? -1 : !n.prerelease && r.prerelease ? 1 : n.prerelease && r.prerelease ? n.prerelease.localeCompare(r.prerelease) : 0;
}
function c(e) {
	let [t] = e.trim().replace(/^v/, "").split("+"), [n, r] = t.split("-", 2), i = n.split(".");
	if (i.length !== 3) return null;
	let [a, o, s] = i.map((e) => Number(e));
	return [
		a,
		o,
		s
	].every((e) => Number.isInteger(e) && e >= 0) ? {
		major: a,
		minor: o,
		patch: s,
		prerelease: r
	} : null;
}
//#endregion
export { e as AGENT_SERVER_VERSION_ERROR_CODE, t as AgentServerFeatureRequirements, n as AgentServerVersionError, o as assertAgentServerSupports, s as compareAgentServerVersions, a as getCachedAgentServerInfo, i as isAgentServerVersionError };

//# sourceMappingURL=agent-server-compatibility.js.map