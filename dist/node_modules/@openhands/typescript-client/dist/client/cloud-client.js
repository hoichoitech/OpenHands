import { HttpError as e } from "./http-client.js";
import { OpenHandsClient as t } from "./openhands-client.js";
import { pollForToken as n, startDeviceFlow as r } from "./device-flow-client.js";
//#region node_modules/@openhands/typescript-client/dist/client/cloud-client.js
var i = "/api/v1/settings/profiles", a = 100, o = class extends t {
	constructor(e) {
		super(e), this.kind = "cloud", this.orgId = e.orgId ?? null, this.proxy = e.proxy ? {
			...e.proxy,
			host: e.proxy.host.replace(/\/+$/, ""),
			path: e.proxy.path ?? "/api/cloud-proxy"
		} : void 0;
	}
	async request(e) {
		return e.hostOverride ? this.requestThroughProxy(e) : this.requestDirect(e);
	}
	startDeviceFlow(e = {}) {
		return r(this.host, e);
	}
	pollForToken(e, t) {
		return n(this.host, e, t);
	}
	async getOrganizations() {
		let e = await this.get("/api/organizations");
		return {
			items: e?.items ?? [],
			currentOrgId: e?.current_org_id ?? null
		};
	}
	getCurrentApiKey() {
		return this.get("/api/keys/current");
	}
	async getOrganizationMe(e) {
		let t = await this.get(`/api/organizations/${encodeURIComponent(e)}/me`);
		return {
			orgId: t?.org_id ?? e,
			userId: t?.user_id ?? "",
			role: t?.role ?? null,
			permissions: Array.isArray(t?.permissions) ? t.permissions : null
		};
	}
	getSettings() {
		return this.get("/api/v1/settings");
	}
	async getSettingsWithDerivedFields() {
		let e = await this.getSettings();
		return {
			...e,
			agent_settings: p(e),
			conversation_settings: m(e),
			llm_api_key_set: !!e.llm_api_key_set,
			search_api_key_set: !!e.search_api_key_set,
			provider_tokens_set: e.provider_tokens_set
		};
	}
	async saveSettings(e) {
		let t = {};
		if (e.agent_settings_diff) {
			let n = { ...e.agent_settings_diff };
			n.agent_context === null && delete n.agent_context, Object.keys(n).length > 0 && (t.agent_settings_diff = n);
		}
		if (e.conversation_settings_diff && Object.keys(e.conversation_settings_diff).length > 0 && (t.conversation_settings_diff = e.conversation_settings_diff), e.app_preferences) for (let [n, r] of Object.entries(e.app_preferences)) r !== void 0 && (t[n] = r);
		await this.post("/api/v1/settings", t);
	}
	getSettingsSchema() {
		return this.get("/api/v1/settings/agent-schema");
	}
	getConversationSettingsSchema() {
		return this.get("/api/v1/settings/conversation-schema");
	}
	listProfiles() {
		return this.get(this.profileBasePath());
	}
	async getProfile(e) {
		let t = await this.get(`${this.profileBasePath()}/${encodeURIComponent(e)}`);
		return {
			name: t.name,
			config: t.config ?? t.llm ?? {},
			api_key_set: t.api_key_set ?? !1
		};
	}
	saveProfile(e, t) {
		return this.post(`${this.profileBasePath()}/${encodeURIComponent(e)}`, t);
	}
	deleteProfile(e) {
		return this.delete(`${this.profileBasePath()}/${encodeURIComponent(e)}`);
	}
	renameProfile(e, t) {
		return this.post(`${this.profileBasePath()}/${encodeURIComponent(e)}/rename`, { new_name: t });
	}
	async activateProfile(e) {
		let t = await this.post(`${this.profileBasePath()}/${encodeURIComponent(e)}/activate`, {});
		return {
			name: t.name,
			message: t.message,
			llm_applied: t.model != null || t.llm != null
		};
	}
	async listSecrets() {
		let e = [], t = null;
		do {
			let n = new URLSearchParams({ limit: String(a) });
			t && n.set("page_id", t);
			let r = await this.get(`/api/v1/secrets/search?${n.toString()}`);
			e.push(...r.items ?? []), t = r.next_page_id;
		} while (t);
		return e;
	}
	async createSecret(e, t, n) {
		await this.post("/api/v1/secrets", {
			name: e,
			value: t,
			description: n
		});
	}
	async updateSecret(e, t, n) {
		await this.put(`/api/v1/secrets/${encodeURIComponent(e)}`, {
			name: t,
			description: n
		});
	}
	async deleteSecret(e) {
		await this.delete(`/api/v1/secrets/${encodeURIComponent(e)}`);
	}
	async listSkills() {
		let e = [], t = null;
		do {
			let n = new URLSearchParams({ limit: String(a) });
			t && n.set("page_id", t);
			let r = await this.get(`/api/v1/skills/search?${n.toString()}`);
			e.push(...r.items ?? []), t = r.next_page_id;
		} while (t);
		return e;
	}
	searchRepositories(e) {
		let t = new URLSearchParams();
		return t.set("provider", e.provider), t.set("limit", String(e.limit ?? 100)), e.query && t.set("query", e.query), e.pageId && t.set("page_id", e.pageId), e.installationId && t.set("installation_id", e.installationId), this.get(`/api/v1/git/repositories/search?${t.toString()}`);
	}
	getInstallations(e) {
		let t = new URLSearchParams();
		return t.set("provider", e.provider), t.set("limit", String(e.limit ?? 100)), e.pageId && t.set("page_id", e.pageId), this.get(`/api/v1/git/installations/search?${t.toString()}`);
	}
	getRepositoryBranches(e) {
		let t = new URLSearchParams();
		return t.set("provider", e.provider), t.set("repository", e.repository), t.set("limit", String(e.limit ?? 30)), t.set("query", e.query ?? ""), e.pageId && t.set("page_id", e.pageId), this.get(`/api/v1/git/branches/search?${t.toString()}`);
	}
	getSuggestedTasks(e = {}) {
		let t = new URLSearchParams();
		return t.set("limit", String(e.limit ?? 30)), e.pageId && t.set("page_id", e.pageId), this.get(`/api/v1/git/suggested-tasks/search?${t.toString()}`);
	}
	searchModels(e) {
		return this.get(`/api/v1/config/models/search${d(e)}`);
	}
	searchProviders(e) {
		return this.get(`/api/v1/config/providers/search${d(e)}`);
	}
	searchConversations(e = 20, t) {
		let n = new URLSearchParams();
		return n.set("limit", String(e)), t && n.set("page_id", t), n.set("sort_order", "UPDATED_AT_DESC"), this.get(`/api/v1/app-conversations/search?${n.toString()}`);
	}
	getConversations(e) {
		if (e.length === 0) return Promise.resolve([]);
		let t = new URLSearchParams();
		for (let n of e) t.append("ids", n);
		return this.get(`/api/v1/app-conversations?${t}`);
	}
	createConversation(e) {
		return this.post("/api/v1/app-conversations", e);
	}
	downloadConversation(e) {
		return this.get(`/api/v1/app-conversations/${e}/download`, { responseType: "blob" });
	}
	async deleteConversation(e) {
		await this.delete(`/api/v1/app-conversations/${e}`);
	}
	updateConversationPublicFlag(e, t) {
		return this.patch(`/api/v1/app-conversations/${e}`, { public: t });
	}
	async pauseSandbox(e) {
		await this.post(`/api/v1/sandboxes/${e}/pause`, {});
	}
	async resumeSandbox(e) {
		await this.post(`/api/v1/sandboxes/${e}/resume`, {});
	}
	getSandboxes(e) {
		if (e.length === 0) return Promise.resolve([]);
		let t = new URLSearchParams();
		for (let n of e) t.append("id", n);
		return this.get(`/api/v1/sandboxes?${t}`);
	}
	readConversationFile(e, t) {
		let n = new URLSearchParams({ file_path: t });
		return this.get(`/api/v1/app-conversations/${e}/file?${n}`, { responseType: "text" });
	}
	async getConversationStartTask(e) {
		let t = new URLSearchParams();
		return t.append("ids", e), (await this.get(`/api/v1/app-conversations/start-tasks?${t}`))?.[0] ?? null;
	}
	async switchConversationProfile(e, t) {
		await this.post(`/api/v1/app-conversations/${e}/switch_profile`, { profile_name: t });
	}
	async switchConversationAcpModel(e, t) {
		await this.post(`/api/v1/app-conversations/${e}/switch_acp_model`, { model: t });
	}
	async listAutomations(e = 50, t = 0) {
		return this.get(`/api/automation/v1?${f(e, t)}`);
	}
	getAutomation(e) {
		return this.get(`/api/automation/v1/${encodeURIComponent(e)}`);
	}
	updateAutomation(e, t) {
		return this.patch(`/api/automation/v1/${encodeURIComponent(e)}`, t);
	}
	async deleteAutomation(e) {
		await this.delete(`/api/automation/v1/${encodeURIComponent(e)}`);
	}
	dispatchAutomation(e) {
		return this.post(`/api/automation/v1/${encodeURIComponent(e)}/dispatch`);
	}
	listAutomationRuns(e, t = 50, n = 0) {
		return this.get(`/api/automation/v1/${encodeURIComponent(e)}/runs?${f(t, n)}`);
	}
	downloadAutomationTarball(e) {
		return this.get(`/api/automation/v1/${encodeURIComponent(e)}/tarball`, { responseType: "blob" });
	}
	getAutomationHealth() {
		return this.get("/api/automation/health", { timeoutSeconds: 5 });
	}
	profileBasePath() {
		return this.orgId ? `/api/organizations/${encodeURIComponent(this.orgId)}/profiles` : i;
	}
	buildUpstreamAuthHeaders(e) {
		let t = e.authMode ?? "bearer";
		return t === "none" ? {} : t === "session-api-key" ? e.sessionApiKey ? { "X-Session-API-Key": e.sessionApiKey } : {} : this.apiKey ? { Authorization: `Bearer ${this.apiKey}` } : {};
	}
	async requestDirect(e) {
		let t = {
			...this.buildUpstreamAuthHeaders(e),
			...this.orgId ? { "X-Org-Id": this.orgId } : {},
			...e.headers ?? {}
		};
		return s({
			host: this.host,
			method: e.method,
			path: e.path,
			params: e.params,
			body: e.body,
			headers: t,
			timeoutMs: e.timeoutSeconds ? e.timeoutSeconds * 1e3 : this.timeout,
			acceptableStatusCodes: e.acceptableStatusCodes,
			responseType: e.responseType
		});
	}
	async requestThroughProxy(e) {
		if (!this.proxy) throw Error("CloudClient proxy options are required for hostOverride requests");
		let t = {
			...this.buildUpstreamAuthHeaders(e),
			...this.orgId ? { "X-Org-Id": this.orgId } : {},
			...e.headers ?? {}
		}, n = {
			"Content-Type": "application/json",
			...this.proxy.apiKey ? { "X-Session-API-Key": this.proxy.apiKey } : {},
			...this.proxy.headers ?? {}
		}, r = this.proxy.path ?? "/api/cloud-proxy";
		return s({
			host: this.proxy.host,
			method: "POST",
			path: r,
			body: {
				host: e.hostOverride,
				method: e.method,
				path: u(e.path, e.params),
				headers: t,
				body: e.body ?? null,
				...e.timeoutSeconds ? { timeout_seconds: e.timeoutSeconds } : {}
			},
			headers: n,
			timeoutMs: this.timeout,
			acceptableStatusCodes: e.acceptableStatusCodes,
			responseType: e.responseType
		});
	}
};
async function s(e) {
	let t = `${e.host.replace(/\/+$/, "")}${u(e.path, e.params)}`, n = {
		...e.body instanceof FormData ? {} : { "Content-Type": "application/json" },
		...e.headers ?? {}
	}, r = {
		method: e.method,
		headers: n,
		signal: AbortSignal.timeout(e.timeoutMs)
	};
	e.body !== void 0 && e.method !== "GET" && (r.body = e.body instanceof FormData ? e.body : JSON.stringify(e.body));
	let i;
	try {
		i = await fetch(t, r);
	} catch (t) {
		throw t instanceof Error && (t.name === "TimeoutError" || t.name === "AbortError") ? Error(`Request timeout after ${e.timeoutMs}ms`, { cause: t }) : t;
	}
	if (!(e.acceptableStatusCodes?.has(i.status) || !e.acceptableStatusCodes && i.ok)) throw await c(i);
	return l(i, e.responseType ?? "auto");
}
async function c(t) {
	let n;
	try {
		n = t.headers.get("content-type")?.includes("application/json") ? await t.json() : await t.text();
	} catch {
		n = null;
	}
	return new e(t.status, t.statusText, n, `HTTP request failed (${t.status} ${t.statusText}): ${JSON.stringify(n)}`);
}
async function l(e, t) {
	if (e.status === 204) return;
	if (t === "blob") return await e.blob();
	if (t === "arrayBuffer") return await e.arrayBuffer();
	if (t === "text") return await e.text();
	let n = e.headers.get("content-type");
	return t === "json" || n?.includes("application/json") ? await e.json() : await e.text();
}
function u(e, t) {
	if (!t) return e;
	let [n, r = ""] = e.split("?"), i = new URLSearchParams(r);
	for (let [e, n] of Object.entries(t)) n != null && (Array.isArray(n) ? n.forEach((t) => i.append(e, String(t))) : i.append(e, String(n)));
	let a = i.toString();
	return a ? `${n}?${a}` : n;
}
function d(e) {
	let t = new URLSearchParams();
	for (let [n, r] of Object.entries(e)) r !== void 0 && t.set(n, String(r));
	let n = t.toString();
	return n ? `?${n}` : "";
}
function f(e, t) {
	let n = new URLSearchParams();
	return n.set("limit", String(e)), n.set("offset", String(t)), n.toString();
}
function p(e) {
	if (e.agent_settings && Object.keys(e.agent_settings).length > 0) return e.agent_settings;
	let t = {}, n = {};
	typeof e.llm_model == "string" && (n.model = e.llm_model), typeof e.llm_base_url == "string" && (n.base_url = e.llm_base_url), typeof e.llm_api_key == "string" && (n.api_key = e.llm_api_key), Object.keys(n).length > 0 && (t.llm = n);
	let r = {};
	return typeof e.enable_default_condenser == "boolean" && (r.enabled = e.enable_default_condenser), typeof e.condenser_max_size == "number" && (r.max_size = e.condenser_max_size), Object.keys(r).length > 0 && (t.condenser = r), typeof e.agent == "string" && (t.agent = e.agent), e.mcp_config && Object.keys(e.mcp_config).length > 0 && (t.mcp_config = e.mcp_config), t;
}
function m(e) {
	if (e.conversation_settings && Object.keys(e.conversation_settings).length > 0) return e.conversation_settings;
	let t = {};
	return typeof e.confirmation_mode == "boolean" && (t.confirmation_mode = e.confirmation_mode), (typeof e.security_analyzer == "string" || e.security_analyzer === null) && (t.security_analyzer = e.security_analyzer), typeof e.max_iterations == "number" && (t.max_iterations = e.max_iterations), t;
}
//#endregion
export { o as CloudClient };

//# sourceMappingURL=cloud-client.js.map