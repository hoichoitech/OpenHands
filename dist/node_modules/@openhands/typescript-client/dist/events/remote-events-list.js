import { HttpClient as e, HttpError as t } from "../client/http-client.js";
//#region node_modules/@openhands/typescript-client/dist/events/remote-events-list.js
var n = class {
	constructor(t, n) {
		this.cachedEvents = [], this.cachedEventIds = /* @__PURE__ */ new Set(), this.client = t instanceof e ? t : new e(t), this.conversationId = n;
	}
	async search(e = {}) {
		let t = { limit: e.limit ?? 100 };
		return e.page_id && (t.page_id = e.page_id), e.kind && (t.kind = e.kind), e.source && (t.source = e.source), e.body && (t.body = e.body), e.sort_order && (t.sort_order = e.sort_order), e.timestamp__gte && (t.timestamp__gte = e.timestamp__gte), e.timestamp__lt && (t.timestamp__lt = e.timestamp__lt), (await this.client.get(`/api/conversations/${this.conversationId}/events/search`, { params: t })).data;
	}
	async count(e = {}) {
		let t = {};
		return e.kind && (t.kind = e.kind), e.source && (t.source = e.source), e.body && (t.body = e.body), e.timestamp__gte && (t.timestamp__gte = e.timestamp__gte), e.timestamp__lt && (t.timestamp__lt = e.timestamp__lt), (await this.client.get(`/api/conversations/${this.conversationId}/events/count`, { params: t })).data;
	}
	async getEventById(e) {
		return (await this.client.get(`/api/conversations/${this.conversationId}/events/${e}`)).data;
	}
	async getEventsById(e) {
		return Promise.all(e.map(async (e) => {
			try {
				return await this.getEventById(e);
			} catch (e) {
				if (e instanceof t && e.status === 404) return null;
				throw e;
			}
		}));
	}
	async addEvent(e) {
		this.cachedEventIds.has(e.id) || (this.cachedEvents.push(e), this.cachedEventIds.add(e.id));
	}
	async append(e) {
		await this.addEvent(e);
	}
	createDefaultCallback(e) {
		return (t) => {
			this.addEvent(t).catch((t) => {
				e && e(t instanceof Error ? t : /* @__PURE__ */ Error(`Error adding event to cache: ${t}`));
			});
		};
	}
	async length() {
		return this.cachedEvents.length;
	}
	async getEvent(e) {
		return this.cachedEvents[e];
	}
	async getEvents(e, t) {
		let n = [], r;
		for (;;) {
			let e = { limit: 100 };
			r && (e.page_id = r);
			let t = (await this.client.get(`/api/conversations/${this.conversationId}/events/search`, { params: e })).data;
			if (n.push(...t.items), !t.next_page_id) break;
			r = t.next_page_id;
		}
		let i = new Set(n.map((e) => e.id)), a = [...n, ...this.cachedEvents.filter((e) => !i.has(e.id))];
		return e === void 0 && t === void 0 ? a : a.slice(e, t);
	}
	async *[Symbol.asyncIterator]() {
		let e = await this.getEvents();
		for (let t of e) yield t;
	}
};
//#endregion
export { n as RemoteEventsList };

//# sourceMappingURL=remote-events-list.js.map