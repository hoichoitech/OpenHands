/** 48px row height — taller than form controls for settings list/table rows. */
export declare const settingsListRowHeightClassName = "h-12 min-h-12";
/** Bordered list shell shared by secrets, LLM profiles, and similar settings tables. */
export declare const settingsListContainerClassName: string;
/** Scrollable variant for long settings lists (e.g. secrets). */
export declare const settingsListScrollContainerClassName: string;
export declare const settingsListDividerClassName = "divide-y divide-[var(--oh-border)]";
/** Interactive row hover on dark list surfaces (base-secondary). */
export declare const settingsListRowHoverClassName = "hover:bg-[var(--oh-interactive-hover-low)]";
export declare const settingsListRowClassName: string;
export declare const settingsListTableRowClassName: string;
export declare const settingsListTableHeadClassName: string;
export declare const settingsListTableHeaderCellClassName: string;
export declare const settingsListTableCellClassName: string;
export declare const settingsListIconActionButtonClassName: string;
