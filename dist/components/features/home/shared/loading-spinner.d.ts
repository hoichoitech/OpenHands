import React from "react";
interface LoadingSpinnerProps {
    hasSelection: boolean;
    testId?: string;
}
export declare function LoadingSpinner({ hasSelection, testId, }: LoadingSpinnerProps): React.JSX.Element;
export {};
