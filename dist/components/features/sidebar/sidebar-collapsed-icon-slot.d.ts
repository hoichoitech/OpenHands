import React from "react";
interface SidebarCollapsedIconSlotProps {
    active: boolean;
    className?: string;
    children: React.ReactNode;
}
/** Collapsed rail: 36px-tall hover/active target aligned with the expanded icon column. */
export declare function SidebarCollapsedIconSlot({ active, className, children, }: SidebarCollapsedIconSlotProps): React.JSX.Element;
export {};
