import React from "react";
interface ErrorMessageProps {
    errorId?: string;
    defaultMessage: string;
}
export declare function ErrorMessage({ errorId, defaultMessage }: ErrorMessageProps): React.JSX.Element;
export {};
