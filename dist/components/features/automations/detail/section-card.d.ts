interface SectionCardProps {
    icon: React.ReactNode;
    title: string;
    children: React.ReactNode;
}
export declare function SectionCard({ icon, title, children }: SectionCardProps): import("react").JSX.Element;
export {};
