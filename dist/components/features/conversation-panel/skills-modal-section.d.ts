import type { ReactNode } from "react";
interface SkillsModalSectionProps {
    title: string;
    count: number;
    children: ReactNode;
}
export declare function SkillsModalSection({ title, count, children, }: SkillsModalSectionProps): import("react").JSX.Element;
export {};
