import { Provider } from "#/types/settings";
interface GitProviderIconProps {
    gitProvider: Provider;
    className?: string;
}
export declare function GitProviderIcon({ gitProvider, className, }: GitProviderIconProps): import("react").JSX.Element;
export {};
