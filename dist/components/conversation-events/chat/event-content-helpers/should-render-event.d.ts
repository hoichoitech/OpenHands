import { OpenHandsEvent } from "#/types/agent-server/core";
export declare const shouldRenderEvent: (event: OpenHandsEvent) => boolean;
export declare const hasUserEvent: (events: OpenHandsEvent[]) => boolean;
